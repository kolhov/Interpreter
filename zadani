Syntaktické prvky:

    5 klic. slov:

    else      elseif    if      then      end

    print(prom)
    read()

    operatory:

    { +, -, *, /, ), (, 0-9 }
    { <, >, <=, >=, ==, != }
    { = }

    function:
    print(prom)
    read()

    promneny:
    prom1

Rozhodování, podmínka je výraz < výraz nebo výraz == výraz.
